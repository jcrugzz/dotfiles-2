{
  "name": basename,
  "description": prompt(),
  "version": "0.0.0",
  "repository": {
    "url": "git://github.com/toddself/"+basename+".git"
  },
  "main": prompt('entry point', 'index.js'),
  "scripts": {
    "postinstall": "if [[ -d .git ]]; then cp pre-commit.sh .git/hooks/pre-commit && chmod +x .git/hooks/pre-commit; fi",
    "pre-commit": "npm run lint && npm test",
    "lint": "jshint",
    "test": "if [[ -d test ]]; then tap test/*js; else tap *test.js; fi"
  },
  "dependencies": {},
  "devDependencies": {
    "tap": "~0.4.4",
    "jshint": "~2.3.0"
  },
  "author": {
    "name": "Todd Kennedy",
    "email": "<todd@selfassembled.org>",
    "url": "http://tck.io"
  },
  "keywords": [],
  "license": {
    "type": "MIT",
    "url": "https://github.com/toddself/"+basename+"/blog/master/LICENSE"
  },
  "engine": {
    "node": ">=0.8"
  }
}